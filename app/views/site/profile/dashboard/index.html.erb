<% if current_member.profile_member.blank? %>
	<h1>Bem vindo ao Dashboard</h1>
<%else %>
	<h1>Bem vindo <%= current_member.profile_member.first_name %></h1>
<% end%>
<br>
<div class="row">
 <div class="col-xs-6">
 	<h4>Seu percentual de serviços é: <%= number_with_precision(@size2, precision: 2)  %>%</h4>
        <span class="pull-right text-muted"><%=@size2 %>% Complete</span>
    </p>
    <div class="progress progress-striped active">
        <div class="progress-bar progress-bar-danger" role="progressbar" aria-valuenow="80" aria-valuemin="0" aria-valuemax="100" style="width: <%=@size2 %>%">
            <span class="sr-only"><%=@size2 %>% Complete (danger)</span>
        </div>
    </div>
</div>
</div>
